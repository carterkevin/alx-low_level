#include "main"

/**
 * _abs - `:` Computes the absolute value od an integer
 * @t: the interger to be computed
 * Return: the absolute valur
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
